# Repository Working Rules

- Never run `git commit` unless the user explicitly requests a commit in the current conversation.
- Never run `git push` unless the user explicitly requests a push in the current conversation.
- Do not treat plan approval, build success, test success, or a request to continue as authorization for commit or push.
- Before committing when explicitly requested, show the proposed change summary and commit message for confirmation if the user asks for review.
